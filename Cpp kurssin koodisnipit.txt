std::string nimi;
std::cout << "Anna nimesi: ";
std::getline(std::cin, nimi);

if (nimi.empty()){
	std::cout << "Moro tuntematon\n";
	
============================
AND		OR	NOT
&&		||	!
============================
